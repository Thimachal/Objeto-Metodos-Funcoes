console.log("-----Calculo do perímetro e área de um círculo-----","\n");
let comprimento = 0, area = 0; raio = 4;

comprimento = 2 * Math.PI * raio;
area = Math.PI * raio * raio;

console.log(`O copmprimento é de ${comprimento.toFixed(2)} e a área ${area.toFixed(2)}%`);